[project]
name = "babarug-visual-studio"
version = "0.1.0"
description = "Moteur de production d'images lifestyle pour tapis d'Orient"
requires-python = ">=3.11"

[project.scripts]
babarug = "babarug.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
filterwarnings = ["ignore::DeprecationWarning"]
